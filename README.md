# Atari-BreakOut-type-game-in-C-Raylib-
Atari breakout type game  made in C using raylib.



Please Ignore the bugs :) (it was my first time using Raylib.) 
<br>
To compile and run this on Linux, use:
```bash
gcc main.c -lraylib -lGL -lm -lpthread -dl -lrt -lX11
